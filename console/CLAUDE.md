# console/ — CLAUDE.md

Atlas Console — a **SwiftUI macOS app** (SP-2 "Console Cockpit"), one Swift Package with three targets. It is a **pure read-face** over `brain watch --json` + the read-class `--json` surface, and a **privileged-flow driver** (render challenge → shell to `atlas-signer` → re-invoke `brain … --authorization`). It opens **no broker socket**, holds **no broker/signing credential**, imports **no atlas internal package**, and handles the egress capability key transiently. Preserve that boundary.

**Outside the pnpm workspace.** `console/` is deliberately not in `pnpm-workspace.yaml`'s globs (`apps/*`, `packages/*`, `tools`). It builds/tests independently: `cd console && swift build && swift test`. Never run `pnpm -r test` for console work; never add it to a pnpm glob.

- **Toolchain:** swift-tools 6.0, `platforms: [.macOS(.v15)]` (Swift 6 language mode / complete concurrency). Swift 6 on PATH.
- **CI:** `.github/workflows/console-ci.yml` (`macos-15`): `swift build` → `swift test` → `scripts/assemble-app.sh`, every step `working-directory: console`.

## Module graph (no back-edges)

`ConsoleCore ← ConsoleUI ← AtlasConsole`. `ConsoleCore` (all non-UI logic) is the leaf; `ConsoleUI` (SwiftUI views) depends only on it; `AtlasConsole` (the `@main` executable, Phase 6) depends on `ConsoleUI`. `ModuleAcyclicityTests` parses the resolved package dump and fails on any library→executable back-edge or a platform other than macOS 15.

## The .app is script-assembled, not xcodebuild'd

A SwiftPM executable has no application target/scheme that emits a launchable bundle, so `scripts/assemble-app.sh` builds `-c release`, lays out `.build/AtlasConsole.app/Contents/{MacOS,Resources}`, installs `Resources/Info.plist` (`CFBundleIdentifier=com.atlas.console`), writes `PkgInfo`, and ad-hoc codesigns. It `cd`s to its own package root first, so it runs correctly from the repo root **or** `console/`. Set `ATLAS_CONSOLE_SCRATCH` to relocate the SwiftPM build dir (tests use this to avoid lock contention with the outer `swift test`); the `.app` always lands at `console/.build/AtlasConsole.app`. That path is gitignored.

## Phase 1 — foundations (`ConsoleCore`)

The substrate everything downstream binds to. Constants live once in `Constants.swift` (`ConsoleConstants` + `BackoffPolicy`).

| File | What it owns |
|------|--------------|
| `ProcessRunner.swift` | The sole subprocess-launch owner. `SpawnRequest`/`SpawnResult`/`SystemProcessRunner`. One-shot `run` drains stdout+stderr **concurrently** (no pipe-capacity deadlock), enforces `timeout` via SIGTERM→SIGKILL reap (`SpawnError.timedOut`), honors cancellation. `executable[0]` MUST be absolute (`Foundation.Process` does no PATH expansion) — a non-absolute token is a typed error. |
| `StreamHandle.swift` | Long-lived reader for `brain watch`: **raw byte chunks** (framing is Phase 2), plus `completion()` → exit code + **full captured stderr** (never swallowed). |
| `SchemaValidator.swift` | Strict runtime JSON-Schema (draft 2020-12 subset) engine. Constraint values read from schema bytes at runtime — nothing hardcoded. Implements exactly the keyword set the atlas schemas use incl. applicators + `unevaluatedProperties` (annotation propagation). `implementedKeywords`/`ignoredKeywords`/`collectKeywords` back the coverage gate. `decode(_:from:)` is the validate-then-`Decodable` wrapper. |
| `ContractBundle.swift` | Binds `commands.json` + per-command `*.schema.json` + `watch`/`error-envelope` schemas from the **checkout that supplies the CLI entry** (`resolve(fromAnchor:)` walks up from `dist/bin.js`, never the `node` launcher). `executionClass` is read from each schema's `x-atlas-contract` (it is **not** on the registry row). |
| `BinaryResolution.swift` | Resolves `brain`/`signer` first-hit-wins (settings → env var → repo-layout default), resolves bare `node` to absolute via the request PATH, probes (`brain db status --json` — a pure command, never an audited read; `atlas-signer pubkey`) under a 10 s timeout, and **fails closed** (`BlockingResolutionError` naming the path + remediation, no fallthrough). V1 same-checkout signer restriction enforced: signer resolution **requires** the already-resolved brain's `contractAnchor` (`resolve(.signer, …, brainAnchor:)`) and derives the bundle/checkout exclusively from it, so a brain from checkout A can never pair with a signer/root from checkout B; the resolved signer's `contractAnchor` is that same brain anchor. Env vars: `ATLAS_ROOT`, `ATLAS_BRAIN_PATH`, `ATLAS_SIGNER_PATH`. |
| `SignerContractValidator.swift` | Transcribed §7.1/§7.2 challenge/response validator (no importable schema). Every required field incl. `schemaVersion`/`payloadCanonicalization`; signature prefix `p256:`/`ed25519:`; **recursively** checks the echoed response challenge equals the committed one. |

## Phase 2 — stream decode, framing, exit interpreters, observability (`ConsoleCore`)

Turns the raw-byte `StreamHandle` into typed, strictly-validated stream items and logs every spawn under an argv allowlist. Nothing here holds a credential or opens a socket — pure transform over Phase-1 primitives + the bound `ContractBundle` schemas.

| File | What it owns |
|------|--------------|
| `WatchEvent.swift` | The 8 event payload structs (each carries the common envelope `at` timestamp — Phase 3 needs `snapshotAsOf = hello.at`) + `WatchEvent` (8 cases + `.unknown(raw:)`) and `WatchEventDecoder`. **Envelope-first decode order:** parse JSON → validate the common envelope (`v`/`event`/`at`, a subschema built from `watch.schema.json`'s shared `$defs`) → **unknown-event check BEFORE the closed union** (a future additive `event` ⇒ `.unknown`, never a decode error) → for a known `event`, validate the full line against that union member (`{"$ref":"#/$defs/<member>","$defs":…}`) then typed-decode. `null` where the contract omits fails at the schema gate. |
| `NDJSONFramer.swift` | Byte-level line framer. Buffers partial lines across chunks and splits on the `\n` byte only, so a multi-byte UTF-8 scalar split across a chunk boundary is reconstructed for free (no decode until a full line is assembled). `push`/`finish`; empty chunks tolerated. |
| `ExitInterpreters.swift` | Two DISJOINT enums — `BrainExit` (0–6) + `SignerExit` (0–5) — with `interpret(_:)` mapping the batch-only `7` to `.aggregateRetryExhausted` and unknowns to `.unrecognized`. Distinct types make a cross-table lookup a compile error. |
| `ErrorEnvelope.swift` | `ErrorEnvelope` (custom `Decodable`: top-level required fields, nested `errors[]` default hint/retryable per the `nestedError` shape) + `ErrorEnvelopeParser` (validate against `error-envelope.schema.json`, then decode). Retry rides `retryable`/`retryAfterMs`; `details` read by field, never by parsing `message`. |
| `StreamTransport.swift` | `StreamItem` (`.event` \| `.terminalEnvelope`) + `WatchStreamParser` (classifies a framed line by the presence of the `event` key; a blank/whitespace-only line is REJECTED — `StreamParseError.blankLine` — never silently skipped) + `WatchTransport` — the **production adapter** wiring `StreamHandle.bytes` → `NDJSONFramer` → `WatchStreamParser` into an `AsyncThrowingStream<StreamItem>` (the one framing pipeline `WatchSupervisor` consumes; `TransportFramingTests` proves four adversarial deliveries yield identical sequences). |
| `ConsoleLog.swift` | `ArgvClassifier` (schema-driven argv allowlist: binary/command/**schema-declared** flag names + structural operands intact; a free-text operand → `<redacted:<cmd> len=NN>` where `NN` is the UTF-8 **byte** count, and a free-text operand spanning **multiple bare tokens** (the NL query) is redacted in full — once a sensitive positional is reached, every following positional is redacted too; a value-flag's VALUE is logged intact ONLY if the schema affirmatively classifies it as structural (numeric-in-`constraint`-range) or enumerated — otherwise redacted (`--k=<redacted:val len=NN>`), so a misplaced secret never lands in a log; sensitive operands keyed by **schema arg name**, position resolved from the schema at runtime; flag/arg declarations are parsed **dual-key** (`name` in the SP-1-era schemas, `flag`/`arg` in the privileged-op schemas — mirroring `OperationRouter.operandKinds`; a third spelling fails the bundle-wide conformance test); **fails closed** — a malformed/unresolved schema redacts every positional, a dash-prefixed or slash-containing operand is redacted not passed through, `--` ends flag parsing, no schema ⇒ redact all operands) + `ConsoleLog` (`os.Logger`, subsystem `com.atlas.console`; `spawn`/`failure` + testable `*Line` builders; raw child stderr is recorded by byte length only via `failure(…, rawOutput:)`, never verbatim — surfaced to error surfaces, never persisted). |
| `LoggingProcessRunner.swift` | The enforcement seam: a **command-agnostic** `ProcessRunner` **decorator** wrapping any inner runner, owning every spawn/termination/failure log call. Command + schema ride each `SpawnRequest` (`command`/`commandSchema`), so ONE wrapped runner sanitizes every command via `ArgvClassifier` — nothing bound at wrap time. `run` emits one launch record (with exit code); `stream` emits one launch record (exit nil) **plus** exactly one `recordTermination` (exit code + byte-length-only stderr) when the streamed process exits. `SpawnLogging` sink protocol + `ConsoleLogSink` (production) make it test-observable. |
| `ProcessRunnerComposition.swift` | The composition root: builds the one shared `SystemProcessRunner`, wraps it once in `LoggingProcessRunner`, and exposes ONLY `runner: ProcessRunner` (never the concrete inner). No API path to an unwrapped runner ⇒ no component can bypass logging (asserted by `testCompositionRootRunnerCannotBypassLogging`). |

## Phase 3 — reducers (`ConsoleCore`)

Pure, fixture-driven transforms over decoded `WatchEvent` payloads (Phase 2). No sockets, no credentials, no atlas-internal imports — a run-space head, a snapshot+live-overlay dashboard, a per-job count map, and resume/cursor selection. Load-bearing invariant across all four: the **disjoint seq spaces** (`run.*` seqs `< DB_EVENT_SEQ_BASE = 10^12` vs the high-space `db.*` + `evidence.retry_enqueued` live-only range).

| File | What it owns |
|------|--------------|
| `AuditReducer.swift` | Run-space-only audit timeline. `apply` accepts an `audit` event into the seq-ordered timeline **iff `seq < DB_EVENT_SEQ_BASE`**; a high-space event routes to `.highSpaceSignal(kind)` (kind classified from `eventType`, unknown kinds surfaced not dropped) and **never** touches the timeline, head, or cursor. `incorporateHello(baselinePrefix:)` (a full re-baseline, called on every hello) seeds the contiguous-prefix head — `-1` = replayAll/fresh/detached, a reported `hello.resume.auditHeadSeq` = existing-ledger baseline (avoids the phantom-gap freeze). `displayedAuditHead == safeCheckpointSeq == max contiguous run-space seq`; a gap is a pending intent (never pruned), so neither advances past it. |
| `DashboardReducer.swift` | `hello.snapshot` overlaid by live events, **one derivation per field**: live-updated (`backup{watermarkSeq,healthy}`, `audit.headSeq` from an embedded `AuditReducer`, daemon reachability, `openRuns` via run-lifecycle deltas) applied only **after** `markCheckpointReached()`; snapshot-only (`quarantineCount`, `backup.coveredSeq`, `audit.anchorOk/anchorSource`) held at the last hello, labelled "as of `snapshotAsOf` = hello.at", **never fabricated as `0`** (absent ⇒ `nil`). Replay-phase run-state deltas are inert (already in `snapshot.openRuns`), so a replayed `run.started` never double-counts. `DashboardState` + `BackupView`/`AuditView`/`DaemonView`. |
| `JobState.swift` | The snapshot-plus-delta job counter. `JobStateMap` recency-merges by `updatedAt` (a list row never clobbers a newer streamed state) and recomputes `JobCounts{queued,failed}` (queued = pending/ready/running, per `health/snapshot.ts`). `JobsListReader.readAll(invoker:)` fully consumes `jobs list --json --limit 500` pagination through the `ReadInvoker` seam (`run(_:args:) -> Data`; phase 6 passes `ReadCommandExecutor.run`, so no raw runner in the reader) — walks `offset` to `hasMore:false`, **dedups by `jobId`**, refuses a non-advancing page. `JobStateCoordinator` (actor) is the **sole** seed/reseed driver + view surface: it owns `pendingBuffer`/`reseedInFlight` so a single read is ever in flight and every event arriving during the read is merged; an unseen `jobId` ⇒ buffer the live event, `readAll`, recency-merge onto **list-confirmed membership only** (never a synthetic insert). Exposes `rows: [JobListRowState]` (list order = `createdAt`-desc/`jobId`-tiebreak, live state overlaid) and `refresh()` (explicit read-on-focus full re-read). |
| `ResumePlanner.swift` | `ResumeMode`/`ResumeArg`. `plan(mode:persistedCursor:)` — `resume` → cursor `>= 0` ⇒ `.sinceSeq(cursor)`, else `.liveOnly`; `replayAll` → `.sinceSeq(-1)`; `liveOnly` → `.liveOnly`. `isStaleCursor(replayEvents:resumeHead:requested:)` = `events==0 && resumeHead < requested` (cursor-above-head only; catch-up-past-cursor is the accepted V1 residual — escape `replayAll`). |

## Phase 4 — watch supervisor & local stores (`ConsoleCore`)

The live-attach control plane: the durable resume cursor, the settings + schema-derived option bounds,
the gated-backoff supervisor, daemon/detach transitions, and the coordinator that threads them into a
checkpointed live stream. Still a pure read-face — no broker socket, no credential, no atlas-internal
import.

| File | What it owns |
|------|--------------|
| `CursorStore.swift` | `CursorStoring` seam (so a test can inject load/checkpoint failures) + the single-writer resume-cursor SQLite store at `~/Library/Application Support/com.atlas.console/console.sqlite` (parent dir `0700`, db `0600`, one `ledger_cursor(incarnation_key PK, audit_head_seq DEFAULT -1, updated_at)` table). `IncarnationKey.derive(ledgerPath:)` = SHA-256 of the absolute path — a re-cloned/re-anchored vault at the same path is a fresh key (fresh `-1`, never a stale cursor). All access serialized behind a lock in a connection holder; `checkpoint` is transactional (`BEGIN IMMEDIATE`/`COMMIT`/`ROLLBACK`). Exposes `checkpointCount` for the sole-writer invariant. `load` returns `-1` for an unseen key. |
| `Settings.swift` | `Settings: Codable` blob (all overrides optional) + `static let defaults` (every optional nil, `.env`, `.resume`). `SettingsStore` over a `UserDefaults` suite distinguishes an **absent** blob (⇒ pure defaults) from a **corrupt** one (⇒ defaults + `.resetFromCorrupt` notice, never a crash/invented value) — presence is checked via `object(forKey:)` FIRST (so a present-but-wrong-type value reads as corrupt, not absent, since `data(forKey:)` returns nil for both). `resolutionInputs()` maps into Phase-1's `ResolutionInputs`. `WatchOptionPolicy` derives `--poll-ms`/`--heartbeat-seconds` ranges + defaults from the bound `watch.schema.json` `x-atlas-contract.flags` `constraint`/`default` (SSOT-derived, fails closed on a missing flag table) — an absent override OMITS the flag so the CLI owns its default, and `watchOptions(from:)` **validates** every persisted override at the settings→argv boundary (a Codable-from-`UserDefaults` out-of-range value is DROPPED, not passed through — proven against both the current and a mutated fixture range). |
| `WatchSupervisor.swift` | `actor WatchSupervisor` — the sole periodically-polling subprocess owner. `classify(exit:envelope:)` is **total** over `(exitCode, envelope?)`: exit 0 ⇒ cleanDetach; exit 5/2 ⇒ terminal (structural); other nonzero decided by the envelope (retryable flag), on-contract code + no envelope ⇒ retryable (dropped stream), **off-contract code + no envelope ⇒ terminal fail-fast**. A framing/decode failure while the child is alive ⇒ terminate + await child ⇒ terminal `contractMismatch` (never hangs on `completion()`). Gated backoff (`BackoffPolicy.delayMs` — 500ms ×2, 30s cap, ±20% jitter, `retryAfterMs` floor) + the consecutive-failure counter reset **only on a proven-healthy run** (a `hello` followed by its first attached `heartbeat`); `WATCH_MAX_CONSECUTIVE_FAILURES = 6` (initial + 5 respawns). Publishes a `stateChanges` `AsyncStream<SupervisorState>` on every transition (`.retrying` carries the ACTUAL failure identity — the envelope `code` / `exit-<n>`, never a literal "retryable"). Terminal states carry the transient diagnostics the contract requires: `.failed(exit,code,hint,stderr)` keeps the envelope `hint` (nil when absent, never fabricated) AND the child's full captured stderr; `.contractMismatch(stage,stderr)` keeps stderr too — neither is discarded. The schema-**unbounded** `retryAfterMs` floor is applied in **integer** space (never `Double(Int.max)`, which would trap on the round-trip) and the `nextAt` banner uses **saturating** `addingReportingOverflow` (a huge floor saturates to `Int.max`, never overflows). The backoff sleep is **wakeable**: `stop()` interrupts an in-flight backoff immediately (never a 30s / unbounded-`retryAfterMs` stall) and reaps the child with a **bounded SIGTERM→SIGKILL escalation** (`reapGrace`, injected short in tests), so a TERM-resistant child never hangs the contract-mismatch reap or `stop()`. `events` stream stays open across `stop()`/re-`run()` so a coordinator can re-attach a new incarnation. |
| `TransitionRouter.swift` | `struct TransitionRouter` turns `WatchEvent`s into `[UISignal]` for the daemon/detach/degraded surfaces. `ReachState.notInstalled` is DERIVED (unreachable AND `socketPath` absent on a read-only stat — never opens a socket; present-but-refusing ⇒ `unreachable`). Emits reachability changes, attach/detach, `reattaching` on `watch.error(source:"ledger")` (which INVALIDATES the cached attach state so the awaited same-ledger `attached` hello re-emits `.attached`, clearing `reattaching`), `anchorDegraded` on `anchorSource:"sqlite-only"`, `backupHealth` (never blocks the stream), and the distinct `serviceNotInstalled` empty state. |
| `AttachCoordinator.swift` | `actor AttachCoordinator` — the SOLE owner of once-hello → cursor → plan → live-spawn → checkpoint. Spawns `watch --json --once` (via `runner.run`), derives the `IncarnationKey` (detached ⇒ skip cursor, plan live-only), loads the cursor, plans the `ResumeArg`, and runs the supervisor. Consumes the supervisor's events, forwarding them, and **checkpoints only on safe attached heartbeats carrying `resume.auditHeadSeq`** (never a pre-replay hello value, never a detached heartbeat) — the sole checkpoint writer. A same-incarnation re-`hello` (startup / post-retry) rebaselines in place; only a real incarnation transition (detached↔attached flip or changed `ledger.path`) calls `supervisor.stop()`, awaits both the process exit and the run-loop task, then re-plans for the new incarnation. Every event is **generation-guarded BEFORE forwarding** (not only before checkpointing): a heartbeat belongs to the active generation iff its own `ledger.path` derives the active incarnation key (attached) or the coordinator is live-only (detached) — an invalidated-generation heartbeat buffered behind a transition hello reaches neither the reducers nor the cursor store. Storage failures are **surfaced, never swallowed**: a `CoordinatorState`/`stateChanges` stream carries `.attaching`/`.live`/`.degraded`(checkpoint failed, non-terminal)/`.failed`(cursor-load or replacement-generation failure, terminal) — a startup cursor-load failure throws out of `start()` and rolls the lifecycle back (`started` stays false, the parked consumer reused) so a retry succeeds, but a **replacement-generation** cursor-load failure **latches** a one-way terminal state (`terminated`): consumption stops, no further generation ever spawns, and a later buffered hello / a re-`start()` (`AttachError.terminated`) can never quietly resurrect a failed coordinator. |

## Phase 5 — privileged flow & egress (`ConsoleCore`)

The most security-sensitive layer: authorizable-op discovery (registry-driven, never hardcoded),
schema-driven routing, the export→sign→authorize state machine, exit-6 backstop, transient egress-key
handling, and the cadence/drift guards. Still a pure read-face + privileged-flow driver — no broker
socket, no held credential, no atlas-internal import.

| File | What it owns |
|------|--------------|
| `OperationRouter.swift` | `AuthorizableOpSet.derive(from:)` scans registry `privilege == "privileged"` at RUNTIME (never a hardcoded list). `OperationDescriptor`/`Operand`/`OperandKind`/`OperandSource`/`OperandRequirement`/`OperandBinding` derive the operand shape from `x-atlas-contract`: positionals (args, `name`/`arg` key, `<>`-stripped), value-flags (`--resolution <…>`), and **boolean switches** (`type:"boolean"`, e.g. `--apply`/`--rollback`/`--reveal`), all EXCLUDING the flow-reserved/preview flags (`--export-challenge`/`--authorization`/`--idempotency-key`/`--dry-run`). `OperandSourceMap.production` (`[op: [name: OperandBinding]]`, `OperandBinding = (source, requirement)`) is the Console-owned focused-UI-vs-operator-vs-**constant** map covering EXACTLY the authorizableOps set (drift-guarded; retired when atlas ships an operand `source` field). `OperationRouter.bind` resolves operands and **enforces requirement/cardinality** — `required` missing ⇒ `missingOperand`, `oneOf(group)` with ≠1 present ⇒ `cardinality` — into an immutable `BoundInvocation` (single argv authority, always incl. `--json`, `exportArgv`/`authorizeArgv` byte-identical apart from the trailing flag). So `purge`/`graduation migrate` always carry the mutating switch (never a preview), `quarantine inspect --reveal` is reachable, `quarantine resolve --resolution` is required. `descriptor(for:)` = nil (⇒ unsupported-privileged-command surface) fail-closed on: the op is not a registry-`privileged` command (never routes a shared command through the privileged flow), no schema, no map entry, OR any drift between the schema's non-reserved operand set and the map's key set — EXACT equality is required bidirectionally (a map entry naming a non-schema operand, OR ANY schema operand — required *or* optional, positional *or* value/boolean flag — absent from the map ⇒ nil, so a stale map can never silently drop an operand). |
| `PrivilegedFlow.swift` | `actor PrivilegedFlow` + `PrivilegedFlowState` + a **`stateChanges: AsyncStream<PrivilegedFlowState>`** publishing EVERY transition (single `transition(to:)` funnel; terminal states delivered) so the UI never polls. A **per-flow `generation`** counter (bumped by `begin`/`handleExit6`/`cancel`) is guarded after every `await` — a continuation resumed after a new begin/cancel can neither revive a superseded flow nor authorize invocation A against flow B's state. The `init` is **throwing** and constructs the `ErrorEnvelopeParser` with `try` (non-optional) — a malformed bound `error-envelope.schema.json` fails the flow at CONSTRUCTION, so the Authorize matrix can never silently disable parsing and downgrade a definitive exit 4/6 into an indeterminate retry. Per-flow temp dir (`0700`, under `~/Library/Caches/com.atlas.console/flows/`); `sweepLeftoverFlows(root:)` sweeps leftovers at launch (wired via `ConsoleLaunch`). Temp-dir creation + `0600` artifact writes are **throwing, MANDATORY, and self-cleaning** (`FlowIOError`) — chmod/stat are required (never swallowed), the mode must be EXACTLY `0700`/`0600` (a missing/uncastable mode REJECTS), and any setup failure removes the locally-created dir/artifact — so the flow fails closed BEFORE any spawn if the dir isn't private or the write didn't land. A superseding `begin` (incl. a routing/unsupported failure) cleans the PREVIOUS flow's dir up front. AuthorizeRetry is published IMMEDIATELY on selecting a retry — before the `retryAfterMs` backoff sleep — so an observer is never stranded in Authorize during the wait. `begin` binds+exports+gates→Display; `confirm` signs→authorizes; `cancel`→Idle. **TOTAL outcome matrix**: Export (exit-6+valid challenge→Display; else Failed, retryable envelope→re-initiate hint); Display consistency gate (`op`/bound-`runId`·`targetCommit`/`payloadCanonicalization`∈`supportedCanonicalizations`) — mismatch ⇒ terminal `challenge-mismatch` BEFORE any artifact is written; Sign branches the DISJOINT `SignerExit` table (0→authorize, 1/2→Failed, 3→re-export, 4→Idle, 5→Failed+re-enroll); Authorize is total over `(brain exit, envelope?)` — exit0→Done (incl. `noop:true`); envelope `nonce_expired`/`nonce_unknown`→re-export (never resubmit stale), `nonce_replayed`→Failed+reconciliation, exit4/6+`retryable`→AuthorizeRetry (same-artifact resubmit, `retryAfterMs` floor, bounded by `maxAuthorizeRetries`); **no parseable envelope ⇒ ONLY exit4/6 is indeterminate-retry — every definitive exit (1/2/3/5) and every UNKNOWN code fails closed, never silently retried**. The exported challenge is read ONCE into frozen bytes piped to `atlas-signer sign` on stdin (no `--out`). `handleExit6(refused:envelope:)` reuses a refused invocation's exact argv (no descriptor, no membership list). The egress key is NEVER injected here (`childEnv` = `ChildEnvironment.nonEgress`). |
| `EgressAction.swift` | `EgressKeyLocation` (`.env(provider)` / `.keychain(service:account:)`, + `processEnv`/`defaultKeychain`) + `EgressKeyProvider.withKey` (reads on demand, drops after the body; keychain path is `SecItemCopyMatching`-only — never Add/Update/Delete; `init(settingsSource:)` maps the persisted `EgressKeySource` choice). `EgressAction.query`/`indexEval(queries:labels:)` spawn the two minting commands with `ATLAS_EGRESS_CAPABILITY_KEY` scoped into the child env for THAT spawn only — `index eval` binds the CLI-**required** `--queries <path>`/`--labels <path>` (a bare `index eval --json` always exits usage) and treats a schema-valid exit-1 `pass:false` report (below-threshold) as a SUCCESSFUL report-bearing outcome (a non-report exit 1 fails the schema gate → `EgressActionError.failed`) (env built from `ChildEnvironment.nonEgress` then the transient key injected — never argv, never persisted, never logged), strict-validate stdout, and on failure return **content-free metadata only** (`EgressActionError.failed(command, exitCode, code, retryable, retryAfterMs, scrubbedStderr)` — NEVER the raw `ErrorEnvelope`, whose message/hint could echo the query; `code` is scrubbed defensively; stderr scrubbed of the operand). `QueryResult`/`IndexEvalResult`/`EgressKeyError`. |
| `ReadSurface.swift` | `ReadSurface.readCommands`/`auditedReadCommands` inventory the 25-command read surface at runtime by `executionClass` (17 read + 4 audited-read + 4 pure) — never a prose list. `CadencePolicy` (sole periodic command = `watch`). `PeriodicScheduler` is the ONE production registration surface — `register(command:)` REFUSES every command but `watch` (`SchedulingError.notPeriodicAllowed`). `WatchSupervisor` ADMITS its recurring `periodicCommand` (default `watch`) through this scheduler at construction, so a supervisor that would periodically spawn an audited read can never be built — the guard is at the real spawn boundary, not a constant checked against itself. `registered` is the audit of what was admitted. `EgressMintingDrift` (mirror = `Constants.egressMintingCommands`; `fromSchemas` returns the authoritative set once a `mintsEgressCapability` field lands, else nil ⇒ the drift test asserts the mirror). |
| `ConsoleLaunch.swift` | The application-startup composition seam: `ConsoleLaunch.performStartupCleanup(flowsRoot:)` (called once by the Phase-6 `@main` before any flow can begin) runs `PrivilegedFlow.sweepLeftoverFlows` so a crash/kill during a prior session can never leave a signed authorization artifact behind. The crash-leftover sweep is WIRED into launch, not merely defined. |
| `ChildEnvironment.swift` | `ChildEnvironment.nonEgress(inherited:overlay:)` — the SINGLE builder for every NON-egress child spawn's environment; it STRIPS `ATLAS_EGRESS_CAPABILITY_KEY` from the inherited env (and refuses an overlay carrying it), so a shell-launched Console never forwards the capability key to a probe/`watch`/read/export/sign/authorize spawn. Only `EgressAction` (re)introduces the key, for its two minting commands. Used by `BinaryResolution` (brain + signer probe), `AttachCoordinator`/`WatchSupervisor` (watch), and `PrivilegedFlow` (`childEnv`). |

## Phase 6 — UI surfaces, accessibility, composition root (`ConsoleCore` + `ConsoleUI` + `AtlasConsole`)

The final phase: the schema-bound read gateway (`ConsoleCore`), the SwiftUI surfaces + accessibility layer
(`ConsoleUI`), and the `@main` composition root (`AtlasConsole`). Still a pure read-face + privileged-flow
driver — no broker socket, no held credential, no atlas-internal import. `ConsoleUI` depends only on
`ConsoleCore`; `AtlasConsole` depends only on `ConsoleUI` (`ModuleAcyclicityTests` enforces no back-edge).

| File | What it owns |
|------|--------------|
| `ConsoleCore/ReadCommandExecutor.swift` | The ONE generic read-on-focus gateway (`ReadInvoker`): looks up the `CommandRow`, REFUSES any command whose `executionClass` is not read/audited-read/pure (`ReadCommandError.notAReadCommand`, BEFORE any spawn — write paths can never ride it), spawns with `--json` (non-egress env), strict-validates exit-0 stdout against the `schemaRef`, and throws the typed error envelope on nonzero (else `unparseableFailure`). `JobStateCoordinator`'s paginator routes through this. The contract bundle is taken **exclusively from `binary.bundle`** (no separate-bundle init param), so a caller can never gate/schema-validate/cwd binary A under bundle B's contract. `Sendable` — builds the envelope parser on demand (the `SchemaValidator` it wraps is not `Sendable`). |
| `ConsoleUI/AppModel.swift` | `LiveSession` (resolved binaries + supervisor/coordinator/flow/egress/readExecutor/router/jobs), `SessionBuilder.build` (shared by `defaultSessionFactory` + tests), `SessionFactory`, `AppPhase`, `BannerState`, and the `@MainActor @Observable AppModel`. **Wire-then-start**: `launch()` loads settings → (setupNeeded if no atlasRoot/override/env) → `sessionFactory` (blocking on `BlockingResolutionError`) → `observe(...)` subscribes the reducer consumer + the three actor `stateChanges` streams FIRST → only then `coordinator.start()`. Mirrors supervisor/flow/coordinator transitions into `@Observable` props + fires `A11yEvent`s (incl. a terminal failed/cancelled announcement on every non-`done` flow exit). `launch()` is **single-flight + idempotent** (a repeated/concurrent launch never builds a second overlapping session/watcher). `select(focus:)`/`selectAuditFocus(_:)` record the current selection so the Actions form pre-fills focused-object operands. **`applySettings`**: gate on an in-flight flow → probe candidate WITHOUT saving → stop old → start replacement → commit only after the replacement's first spawn → on failure rebuild+restore the prior config and keep the prior persisted settings; an unchanged save never re-probes. `refreshJobs`/`beginAction`/`confirmChallenge`/`cancelFlow`/`runQuery` are the explicit user-action surfaces (no timer). |
| `ConsoleUI/Surfaces.swift` | `DashboardView`/`JobsListView`/`AuditTimelineView`/`ModelCallFeedView` + `StatusBadgeView`. Audit timeline + model-call feed are `Table` (list) forms — no chart-only encoding. Retroactive `Identifiable` on `AuditPayload`/`ModelCallPayload`. |
| `ConsoleUI/Presentation.swift` | The pure, testable accessibility substance: `StatusBadge` (symbol + text + tint — never color-only), `StatusPresentation` (reachability/jobState/backup/anchor), `DashboardPresentation.asOfLabel`/`snapshotOnly` (absent ledger-derived value ⇒ "as of", never a fabricated `0`). |
| `ConsoleUI/ControlSafeText.swift` | `ControlSafeText.render` (quoted `AttributedString`) / `.plain` (accessibility-label `String`): every threat-relevant scalar → a visible `<U+XXXX>` token; NEVER truncates. Unsafe is decided **by Unicode general category, fail-closed** (Cc/Cf/Zl/Zp ⇒ tokenized, + pinned U+180E) — covering C0/DEL/C1, the line/paragraph breakers, the full bidi override/isolate/mark set, the zero-width joiners/BOM/soft-hyphen, **and** every invisible format control an enumerated blocklist would miss (the TAG block U+E0001/U+E0020–E007F, U+206A–206F, …) — a challenge value can never hide a committed suffix by rendering invisibly. |
| `ConsoleUI/A11y.swift` | `A11yEvent` (jobSucceeded/failed, backupUnhealthy, daemonUnreachable, challengeArrived/Expired, watchRetrying, watchFailed, busy/completed, and the terminal **failed/cancelled** outcomes that close out `busy`) with control-safe utterances + `A11yAnnouncer` (injectable `post` sink; production posts a SwiftUI `AccessibilityNotification.Announcement`) + `A11yStructure` (the declared heading/landmark structure + the complete announcement vocabulary, asserted host-independently). |
| `ConsoleUI/Banners.swift` | `RetryBanner`/`WatchFailedView` (exit+code+hint+captured stderr, none discarded)/`ServiceNotInstalledView`/`QuarantineBadge`/`EvidenceRetryBadge`. Retroactive `Identifiable` on `JobListRowState`. |
| `ConsoleUI/ChallengeDisplayView.swift` | `ChallengePresentation.fields` (the full §7 display set — op, runId/targetCommit when present, canonicalBaseCommit, every `intendedEffect` field, expiresAt, and the `sha256:` of `signingPayload` — control-safe + full-length). `intendedEffect` values render as **type-preserving JSON** (strings quoted, so `"true"` ≠ boolean `true` and delimiters can't spoof structure); each `Field` carries a **unique `id` derived from the raw key** (SEPARATE from the collision-prone display label), so distinct hostile keys that escape to the same label never yield duplicate SwiftUI IDs / dropped rows. + `ChallengeDisplayView` (renders from the FROZEN challenge; confirm/cancel). |
| `ConsoleUI/ActionQuerySettings.swift` | `ActionsView` (enumerates `authorizableOps` → operand form → `beginAction`; challenge modal via sheet), `QueryView` (explicit run → `runQuery`, never polled), `SettingsView` (edits `ConsoleCore.Settings`; Apply → `applySettings`). |
| `ConsoleUI/MainWindow.swift` | The root scene content: switches on `AppPhase` (probing/setupNeeded/blocked/running), a tabbed cockpit of the surfaces, the banner bar, and `SetupNeededView`/`BlockedView`. |
| `AtlasConsole/AtlasConsoleApp.swift` | The `@main` SwiftUI `App`. `init()` calls `ConsoleLaunch.performStartupCleanup()` FIRST (crash-leftover sweep before any flow), then constructs `AppModel`; the scene is a **singleton `Window`** (never a `WindowGroup` — a second window would run `launch()` again and spawn an overlapping session/watcher); the root `.task` runs the single-flight `launch()`. |

`ConsoleCore` additive helpers this phase needs: a **validating, throwing** `ResolvedBinary.init` (`ResolvedBinaryError` — rejects an empty/relative launch or a `contractAnchor` outside the bound `bundle.checkoutRoot`, so no fixture can bypass the resolver's probe/binding invariants) and `JobListRowState.init` (UI/test construction). `WatchSupervisor.awaitReady()` resolves `true` on the first successful `runner.stream()` or `false` on a terminal-before-spawn, and `AttachCoordinator.start()` gates its initial completion on it (a stream-launch failure never reports success). `AttachCoordinator.enableConsumerAcks()`/`consumed()` add the consumption-before-checkpoint barrier: with acks enabled the coordinator will not checkpoint the cursor until the consumer has acknowledged every event yielded so far. `ControlSafeText` uses JSON-style quoting — `"`/`\` are backslash-escaped so a value can never terminate its own quote — and `ChallengePresentation` control-safe-escapes intendedEffect KEYS in labels too. `AppModel.applySettings` is atomic: a candidate that fails to start is torn down fully and the prior config is restored only after a verified start (else the session is cleared and the app enters `.blocked`); a valid apply from `.setupNeeded`/`.blocked` reaches `.running` without a relaunch; challengeExpired/busy/completed announcements fire on real read/query/privileged transitions. `ActionOperandForm` builds the Actions surface's operand controls from the `OperationDescriptor` (focused operands pre-filled from `selectedFocus`), and `RetryBanner`/`RestoreBanner` render attempt/next-retry/last-code and the backup-restore surface.

## Tests

`cd console && swift test`. All Phase-1 suites run in CI (no Secure Enclave, no real broker — software fixtures + a scripted-spawn harness). `TestSupport` locates the repo checkout from `#filePath` and builds throwaway fixture checkouts. Named Phase-1 suites: `ModuleAcyclicityTests`, `AppBundleIdentityTests`, `ProcessRunnerTests`, `StreamHandleTests`, `SchemaValidatorTests`, `SchemaKeywordCoverageTests`, `SchemaDecodeTests`, `ContractBundleResolutionTests`, `PathResolutionProbeTests`, `NodeResolutionTests`, `SignerContractValidatorTests`. Named Phase-2 suites: `WatchEventDecoderTests`, `UnknownEventToleranceTests`, `NDJSONFramerTests`, `ExitInterpreterTests`, `ErrorEnvelopeTests`, `TransportFramingTests` (real subprocess-pipe framing under adversarial chunking), `ArgvSanitizationTests`. Named Phase-3 suites: `MixedSpaceReducerTests`, `LiveOnlyExistingLedgerBaselineTests`, `DashboardReducerTests`, `CoalescingJobCountTests`, `ResumeReplayTests`, `StaleCursorRebaselineTests`, `RebaselineOnHelloTests` (fixtures + a scripted `jobs list` `ReadInvoker` — `ScriptedReadInvoker` in `ReducerFixtures.swift`, with an `onPage` pause hook for the paused-paginator concurrency test, plus `GatedReadInvoker` (a read that parks until `release()`) for the concurrent-seed/refresh in-flight-await + failure tests — payloads built directly since decoding is proven in `WatchEventDecoderTests`). Named Phase-4 suites: `CursorStoreTests`, `LiveOnlyCheckpointSurvivesRestartTests`, `SettingsStoreTests`, `WatchSupervisorTests`, `DaemonTransitionTests`, `AttachResumeCheckpointFlowTests` — the supervisor + coordinator suites drive a `ScriptedSpawnRunner` (`WatchTestHarness.swift`) that pops per-`stream()` behaviors and launches real emitter shell scripts (real pipes/exit codes, like `TransportFramingTests`) while returning canned `--once` stdout with no subprocess; `Fx4` builds schema-valid NDJSON lines from the `watch.schema.json` examples. The supervisor's backoff `sleeper` is injected so backoff is asserted deterministically without real waits, and `CursorStore.checkpointCount` proves the exactly-one-checkpoint-at-the-heartbeat invariant. `WatchSupervisorTests` also asserts the `stateChanges` stream (retry attempt/nextAt/real lastCode), a prompt `stop()` during `.retrying` (blocking sleeper, no respawn), and a TERM-resistant child reaped via the bounded SIGTERM→SIGKILL escalation (`ScriptedSpawnRunner.emitThenIgnoreTerm`, short `reapGrace`). `AttachResumeCheckpointFlowTests` covers the generation-guard drop of a stale pre-transition heartbeat and injected cursor-load/checkpoint failures (`ToggleFailStore`) surfacing terminal/degraded states + a start-rollback-then-successful-retry. The flow tests poll generously (30 s) because they launch real emitter subprocesses under the suite's parallel load. Named Phase-5 suites: `PrivilegedOpDiscoveryRoutingTests` (registry-driven discovery, byte-identical export/authorize argv, `operandSourceMapCoversAuthorizableOps` stale-entry guard, a flipped `shared`→privileged fixture row discovered + bindable, the descriptor-less unsupported surface, the exit-6 backstop reusing exact argv; **every production privileged op's COMPLETE argv** — booleans + required + cardinality: `purge`/`graduation migrate` always carry the mutating switch, `quarantine inspect --reveal` reachable, `quarantine resolve --resolution` required; and **bidirectional mutated-schema drift** — an added required schema operand absent from the map ⇒ descriptor nil, a map entry naming a non-schema operand ⇒ nil), `PrivilegedFlowExitTableTests` (the full brain-0–6 / signer-0–5 outcome matrix with software-P-256 fixtures + temp-dir cleanup on every terminal path; **definitive authorize exits 1/2/3/5 + UNKNOWN with no envelope FAIL, only 4/6 retry**; the `stateChanges` stream publishes export→display→sign→authorize→authorizeRetry→done + cancellation-idle terminal; an unwritable flows root fails closed BEFORE any spawn; a `cancel` mid-authorize is not revived by the old continuation via `GatedAuthorizeRunner`), `BrokerRestartAuthorizeRetryTests` (nonce_expired/unknown→re-export never resubmit, commit-then-response-loss→same-artifact resubmit→Done, nonce_replayed→reconciliation, retryable→resolve, indeterminate-past-cap→Failed), `EgressKeyScopingTests` + `FailingQueryRedactionTests` (key injected only for the two minting commands, absent from every other spawn; `ChildEnvironment.nonEgress` strips the key from every read/privileged command via the runtime inventory; a GENUINELY inherited key (`setenv`) never reaches a privileged spawn yet `EgressAction` still injects its transient key; keychain source read-only via `SecItemCopyMatching` (missing item ⇒ not-found, never a mutation); never in argv/UserDefaults, env-missing keychain hint; a failing query's operand — incl. regex/newline/emoji variants — scrubbed from BOTH the unified log and the content-free error surface), `CadenceGuardTests` + `EgressMintingDriftTests` + `ReadCommandConformanceTests` + `LaunchCleanupTests` (`ConsoleLaunch.performStartupCleanup` removes stale flow dirs at launch; `WatchSupervisor` admits watch but is NOT constructible for any audited read; `index eval` below-threshold exit-1 report returned, non-report exit-1 fails; a shared command / an unmapped optional operand ⇒ descriptor nil; a superseding invalid begin cleans the prior temp dir; a malformed bound envelope schema fails flow construction; AuthorizeRetry is published under a blocking sleeper; the real once/streaming-watch + export/sign/authorize callers strip an inherited key; `PeriodicScheduler` admits only watch and REFUSES every audited/read command from the runtime inventory, minting-constant vs schemas, representative read commands carry `--json` on their argv contract + an ARMED validator — a payload missing a required field is rejected). The privileged-flow harness (`PrivFlowHarness.swift`) scripts the three spawns by ROLE (`--export-challenge`/`--authorization`/`sign`) via `PrivRunner`, and `PFx` builds schema-valid challenge/response bytes + a `p256:` signature; `RecordingRunner` (in the egress suite) captures per-spawn env/argv for the scoping assertions and `CapturingLogSink` captures every `SpawnLogging` record for the leak assertion.

Named Phase-6 suites: `ReadCommandExecutorTests` (in `ConsoleCoreTests` — strict success across each read class read/audited-read/pure, typed error-envelope throw, exit-0-invalid-output rejection, and a projection-write/mutating/unknown command refused BEFORE any spawn). In `ConsoleUITests` (a self-contained `UITestSupport` + `UIScriptedRunner`, since `ConsoleCoreTests`' harness is not visible across targets): `SurfaceRenderTests` (each surface constructs from fixture reducer state; snapshot-only "as of" labels; the cadence guard admits only `watch`), `ChallengeRenderingTests` (a hostile C0/C1/ANSI/RTL + over-length challenge renders the full §7 set incl. the signing-payload SHA-256, every value control-safe and full-length, no raw control byte reaching a rendered value), `AccessibilityAcceptanceTests` (no color-only encoding — distinct symbol+text per state; control-safe accessibility text; a complete, control-safe announcement vocabulary; the capturing announcer), `AppLaunchProbeTests` (setupNeeded / blocked-naming-path-and-remediation / running-with-watch-spawn), `WiringOrderTests` (a fast emitter's hello/audit/heartbeat all reach the reducers — none outruns the pre-wired consumer), `SettingsCutoverTests` (candidate-probe-failure saves nothing, replacement-start-failure restores the prior + retains settings, success commits only after the replacement spawn, an unchanged save never re-probes), `ActionSurfaceTests` (a UI-initiated op drives `PrivilegedFlow.begin` to Display through the scripted spawn boundary; the query surface invokes `EgressAction.query` only on the explicit action), `ActorStateStreamTests` (a supervisor retry transition + a flow challenge transition each reach the mirrored `@Observable` props and fire the matching `A11yEvent`). Revision-round suites: `SettingsCutoverAtomicityTests` (restoration factory/start failure ⇒ session cleared + `.blocked`, a live restored watcher on candidate-start failure, a stream-launch failure never committed, exactly one watcher after a successful cutover, `.setupNeeded`/`.blocked`→`.running` without relaunch, non-default persisted settings exposed), `CheckpointConsumptionOrderTests` (a `CursorStoring` checkpoint spy proves the preceding audit row was consumed BEFORE the cursor advanced), `ResolvedBinaryInitTests` (empty/relative launch + cross-checkout anchor rejected), plus the `ActionSurfaceTests` UI-path bind of every production authorizable op + the challengeExpired/busy/completed announcement paths, and the `ChallengeRenderingTests` hostile-quote/backslash/control-key escaping cases. Round-3 revision additions: `ReadCommandExecutorTests.testGatingSchemaAndCwdComeFromBinaryBundle` (the executor's bundle is `binary.bundle` exclusively); `AttachResumeCheckpointFlowTests.testStopWithWithheldHeartbeatAckDoesNotCheckpoint` (stop() ABORTS a withheld-ack checkpoint, never advancing past unconsumed state); `AppLaunchProbeTests.testRepeatedAndConcurrentLaunchProducesExactlyOneWatcher` (single-flight launch); `ChallengeRenderingTests.testMutatingChallengeArtifactAfterDisplayDoesNotChangeSignedBytes` (drive PrivilegedFlow to Display, tamper `challenge.json`, confirm, assert the signer stdin == the displayed bytes — the #253 mutation-after-confirm criterion, via `UIScriptedRunner.signStdins`); `ActionSurfaceTests.testAuditSelectionPreFillsFocusedOperandOnActionsForm` + `testFailedAndCancelledFlowAnnounceTerminalOutcomes`; `SurfaceRenderTests` rendered-content + `testRefreshDrivesJobsListThroughTheReadExecutor` (a recording runner proves the focus/action refresh spawns `jobs list` through the executor); `AccessibilityAcceptanceTests` names/roles substance + declared heading structure + exhaustive-vocabulary (incl. failed/cancelled) + no-truncation policy. The genuinely host-requiring accessibility checks (Full Keyboard Access end to end, modal focus entry/restoration, Dynamic Type / Reduced Motion / contrast on the running app, the live VoiceOver pass) are the **manual accessibility checklist** ([`docs/specs/2026-07-19-console-accessibility-manual-checklist.md`](../docs/specs/2026-07-19-console-accessibility-manual-checklist.md), #254), since a SwiftPM package has no XCUI host. (The #257 live-drive retro is deferred — `ai:human-led` — and is NOT scaffolded here.)

Normative sources: [`docs/specs/2026-07-19-console-cockpit-spec.md`](../docs/specs/2026-07-19-console-cockpit-spec.md) (contract) and [`docs/plans/2026-07-19-console-cockpit-plan.md`](../docs/plans/2026-07-19-console-cockpit-plan.md) (six-phase plan). SwiftPM's `--filter` is singular — use one regex or the unfiltered suite, never repeated `--filter` flags.
